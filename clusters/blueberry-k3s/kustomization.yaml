apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Flux core components and GitRepository source
  - flux-system
  # Infrastructure platform components
  - infrastructure.yaml
  # Application workloads (Matrix stack, etc.)
  - apps.yaml
