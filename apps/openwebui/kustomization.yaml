apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Namespace must exist before all other resources
  - namespace.yaml
  # Ollama backend URL (SOPS-encrypted)
  # ⚠️  This file MUST be SOPS-encrypted before committing. See README.md.
  - openwebui-secrets.yaml
  # OpenWebUI: PVC, Deployment, Service
  - openwebui.yaml
