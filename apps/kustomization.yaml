apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Matrix homeserver stack (Synapse + Element + PostgreSQL)
  - matrix/
  # OpenWebUI: browser-based LLM chat UI backed by Ollama on macserver
  - openwebui/
